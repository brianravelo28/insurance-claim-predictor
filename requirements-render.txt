# Runtime dependencies for the hosted dashboard (no training stack). Versions match the local
# environment the app was tested in, and the Rossmann dashboard's working Render deploy.
pandas==2.0.3
numpy==1.24.3
pyarrow==21.0.0
lightgbm==4.6.0
plotly==5.14.0
dash==4.4.0
gunicorn==23.0.0
