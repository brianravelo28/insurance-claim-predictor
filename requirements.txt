# Full pipeline + dashboard (data download, cleaning, storm join, training, app).
# Versions are the ones this project was built and tested with (Python 3.9+; hosted build uses requirements-render.txt).
pandas==2.0.3
numpy==1.24.3
scikit-learn==1.3.0
lightgbm==4.6.0
pyarrow==21.0.0
requests==2.32.3
plotly==5.14.0
dash==4.4.0
